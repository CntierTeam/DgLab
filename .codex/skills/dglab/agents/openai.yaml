interface:
  display_name: "DgLab Coyote"
  short_description: "Use Coyote V2/V3 via dglab"
  default_prompt: "Use $dglab to control or demo a Coyote (install, TUI, or --mock --headless scripts). 仅学习用."
policy:
  allow_implicit_invocation: true
