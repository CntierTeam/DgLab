interface:
  display_name: "DgLab"
  short_description: "How to use dglab CLI/TUI"
  default_prompt: "Use $dglab to explain or run the DgLab CLI/TUI (keys, headless commands, emergency stop). Product usage only."
policy:
  allow_implicit_invocation: true
